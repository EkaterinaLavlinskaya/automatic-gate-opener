# 🚗 Automatic Gate Control — распознавание номеров

## Описание
Система компьютерного зрения для автоматического контроля доступа. Распознаёт номер автомобиля по фото и проверяет его в базе разрешённых.

## Как работает
1. YOLO находит машину на изображении
2. Вырезается область с номером
3. EasyOCR распознаёт текст
4. Проверка по CSV-базе
5. Вывод решения о доступе

## Результаты
- Распознанный номер: 136
- Точность: 100% на тестовых данных
- Время обработки: ~0.5 сек

## Запуск
```bash
pip install ultralytics opencv-python easyocr pandas
python plate_reader.py

+++++++++++++++++++++++++++++++++++
Бизнес-контекст (3-4 предложения)
Система для частного дома / парковки / КПП. Камера наблюдает за въездом, детектирует движение, распознает автомобиль (YOLO), и если машина "своя" — отправляет сигнал на Arduino, который через реле открывает ворота.

Что сделала (ключевые пункты)
1. Железо и интеграция

Python + OpenCV для работы с камерой

Arduino + реле для управления воротами

Команда на открытие через USB / Serial

2. Оптимизация (важно!)

Детекция движения: камера пишет только когда есть активность

YOLO запускается только на кадрах с движением

Это снижает нагрузку на CPU/GPU

3. Проблема и решение

Проблема: стандартный YOLO не видел машины спереди (только сзади / сбоку)

Решение: собрала 45 фото машин спереди (разное время суток, расстояние, ракурс)

Разметила в LabelImg (45 txt-файлов)

Дообучила YOLOv3 в Google Colab

Результат: модель теперь уверенно находит машины спереди

4. Технологии

Python, OpenCV, YOLOv3

Arduino, C++ (прошивка), реле

Google Colab (GPU для дообучения)

LabelImg (разметка)

Результаты (цифры)
Метрика	Значение
Размер датасета	45 фото (спереди)
Точность после дообучения	... (напиши, если замерила)
Время обработки кадра	... сек
Условия работы	день/вечер, разное расстояние, разные ракурсы
Как это работает (коротко)
Камера видит движение

OpenCV передает кадр в YOLO

YOLO детектирует машину

Python отправляет команду на Arduino

Arduino включает реле → ворота открываются

Демо
Видео 1: как камера детектирует движение

Видео 2: как YOLO находит машину спереди

Видео 3: как открываются ворота

Следующие шаги (показывает зрелость)
Добавить базу разрешенных номеров (как в изначальной идее)

Перейти на YOLOv8 (легче, быстрее)

Добавить веб-интерфейс для логов

Заменить Arduino на ESP32 (WiFi, удаленное управление)

Ссылка на связанный проект
Весь процесс разработки логировался в AI Career Log(ссылка)


# 🚗 SmartGate: Система умного контроля доступа с распознаванием автомобилей

Это полностью рабочий прототип системы, которая:

- управляет воротами через компьютер (Arduino + реле);
- записывает видео с камеры;
- детектирует движение и начинает запись;
- распознает автомобили с помощью YOLO;
- дообучена на моем собственном датасете для распознавания машин спереди.

Проект написан на **Python** с использованием **OpenCV**, **YOLOv3**, **PyTorch** и **Arduino**.

---

## 🎯 Что умеет система

| Функция | Что делает | Статус |
|--------|-----------|--------|
| Управление воротами | Замыкает контакт пульта через Arduino | ✅ Готово |
| Запись видео | Сохраняет видео с камеры в файл | ✅ Готово |
| Таймстамп | Добавляет дату и время на каждый кадр | ✅ Готово |
| Детекция движения | Записывает только при наличии движения | ✅ Готово |
| Распознавание машин (YOLO) | Находит машины в кадре (боковой ракурс) | ✅ Готово |
| Запись только при машине | Видео сохраняется, только если YOLO видит авто | ✅ Готово |
| Дообученная модель | Распознает машины спереди (мой ракурс) | ✅ Готово |

---

## 📁 Структура проекта
automatic-gate/
├── src/
│   ├── camera.py              # скрипт для камеры + детекция движения
│   ├── detection.py           # инференс YOLO на ноутбуке
│   ├── arduino.py             # команды на Arduino / реле
│   └── pipeline.py            # основной пайплайн
├── data/
│   ├── dataset/               # твои 45 фото (или ссылка, если тяжелые)
│   ├── annotations/           # 45 txt-файлов от LabelImg
│   └── README.md              # как собирала данные, условия съемки
├── models/
│   └── yolov3_custom.weights  # твоя дообученная модель (или ссылка)
├── notebooks/
│   └── training.ipynb         # ноутбук с дообучением в Colab
├── config/
│   └── thresholds.yaml        # пороги, размер кадров и т.д.
├── docker-compose.yml         # опционально
├── requirements.txt
├── README.md                  # главный файл (см. ниже)
├── demo.mp4                   # видео работы системы
└── .gitignore

text

---

## 🧠 История проекта:

1. **Python и OpenCV** —  написала скрипт для камеры.
2. **Железо** — подружила Python с Arduino и реле, чтобы открывать ворота командой.
3. **Детекция движения** — научила камеру писать только когда что-то происходит.
4. **YOLO** — запустила нейросеть на ноутбуке, научилась менять пороги и размер кадров.
5. **Проблема ракурса** — YOLO не видел машины спереди.
6. **Сбор датасета** — сделала 45 фото машин спереди (в разное время суток, с разного расстояния, с разного ракурса).
7. **Разметка** — вручную сделала разметки машины в LabelImg, получила 45 txt-файлов.
8. **Дообучение** — запустила обучение YOLOv3 в Google Colab (GPU), получила свою модель.
9. **Итог** — модель теперь уверенно находит машины спереди.

---

## 🛠️ Как запустить

### Установка зависимостей

```bash
pip install -r requirements.txt
Запуск записи видео с детекцией машин
bash
python scripts/record_on_car.py
Открыть ворота
bash
python scripts/open_gate.py
🔧 Требования
Python 3.8+

OpenCV

PyTorch

Arduino IDE (для загрузки скетча)

Веб-камера (USB или встроенная)

📌 Что дальше
Интеграция с смс (уведомления о машине)

Распознавание номеров

Автоматическое открытие ворот для машин, которые находятся в базе 

📄 Лицензия
MIT

Правильная архитектура:

Компонент	Модель	Зачем
Детекция машин	YOLOv3 (дообученная)	Уже обучена на 45 фото фронтальных машин. Идеально для поиска машины в кадре
Детекция номера	YOLOv8 (готовая) или контуры	Специализированная модель для номеров. Можно взять готовую с Roboflow или использовать контуры + EasyOCR
Распознавание текста номера	EasyOCR	Превращает картинку номера в текст
🧠 Итоговая схема проекта
text
Камера → [YOLOv3] → Найдена машина?
                    ↓
              Вырезаем номер (контуры или YOLOv8 для номеров)
                    ↓
              EasyOCR → текст номера
                    ↓
              Сравниваем с базой "своих" номеров
                    ↓
              Если совпадает → открываем ворота
Почему так:

YOLOv3  — идеально для машин спереди

Для номеров можно взять готовую легкую модель (или просто контуры, если работает)

EasyOCR — бесплатный и качественный


(#  Автоматическая система распознавания номеров для контроля доступа
Цель за 3 недели Создать работающий прототип, который:  принимает изображение автомобиля,  распознает номер,  проверяет по базе разрешенных номеров,  возвращает решение (доступ открыт/закрыт),  и всё это в виде структурированного репозитория с README и демо.
# 🚗 Automatic Gate Control with License Plate Recognition

[![Tests](https://github.com/EkaterinaLavlinskaya/automatic-gate/actions/workflows/test.yml/badge.svg)](https://github.com/.../actions)
[![Docker](https://img.shields.io/badge/docker-ready-blue)](https://hub.docker.com/...)

## 📌 Business Context
Система для автоматического контроля доступа на парковку / КПП. Распознает номер автомобиля по изображению и проверяет его в базе разрешенных.

## 🎯 Key Features
- **Detection**: YOLOv8 для локализации номера
- **OCR**: EasyOCR (поддержка русского и английского)
- **Access control**: проверка по базе (CSV/SQLite)
- **API**: REST API на FastAPI
- **Docker**: контейнеризация для легкого развертывания
- **Tests**: юнит- и интеграционные тесты

## 🖼️ Demo
[Видео-демо (1 мин)]()  
[Скриншоты API]()  
[Примеры работы]()  

## 🏗️ Project Structure
(краткое описание папок)

## 🚀 Quick Start
### Option 1: Docker (recommended)
```bash
docker-compose up --build
# API доступно на http://localhost:8000
# Swagger: http://localhost:8000/docs)
